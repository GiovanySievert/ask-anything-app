import SwiftUI

enum AppColors {
  static let background = Color.black
  static let surface = Color.white.opacity(0.10)
  static let primaryText = Color.white
  static let secondaryText = Color.white.opacity(0.62)
  static let accent = Color.white

  static let glassBorder = Color.black.opacity(0.18)
  static let glassOverlayTint = Color.black.opacity(0.5)

  static let sendButtonIdle = Color.white.opacity(0.35)
  static let sendButtonActive = Color.white

  static let userBubble = Color.white
  static let userBubbleText = Color.black
  static let assistantBubble = Color.white.opacity(0.14)
  static let assistantBubbleText = Color.white
}
